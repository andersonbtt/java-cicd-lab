server:
  port: 8080

spring:
  application:
    name: labjavacicd

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always

info:
  app:
    name: labjavacicd
    description: Laboratorio Java CI/CD + ELK no Kubernetes
